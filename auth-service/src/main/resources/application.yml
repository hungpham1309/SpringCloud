spring:
  datasource:
    driverClassName: com.microsoft.sqlserver.jdbc.SQLServerDriver
    url: jdbc:sqlserver://localhost:1433;database=UserDB
    username: sa
    password: Chanhhung96
    initialize: false
  application:
    name: auth-service
eureka:
  client:
    service-url:
      default-zone: ${EUREKA_URI:http://localhost:8761/eureka}
server:
  port: 8083

